{{#each thread.comments as |comment|}}
  {{#liquid-if (not comment.isNew)}}
    {{comment-list-item comment=comment}}
  {{/liquid-if}}
{{/each}}

{{comment-form thread=thread on-submit=(action 'addComment')}}
